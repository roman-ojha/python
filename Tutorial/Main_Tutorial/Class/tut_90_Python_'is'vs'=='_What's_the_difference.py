

# == - value equality - Two objects have the same value
# here, == means the value of the object are same
# is - reference equality - Two references refer to the same object
# so is means two object are same


# Task:
a = [6, 4, "34"]
b = [6, 4, "34"]
print(b is a)
